import Beranda from "./Beranda";

const BerandaConfig = {
    settings: {
      layout: {
        config: {},
      },
    },
    routes: [
      {
        path: '/beranda',
        component: Beranda,
      },
    ],
  };
  
  export default BerandaConfig;